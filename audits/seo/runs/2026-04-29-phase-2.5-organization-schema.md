# Phase 2.5 run — Organization schema on homepage

Date: 2026-04-29

## Result

Added a second `<script type="application/ld+json">` block to `index.html` immediately after the existing `WebSite` schema. The new block is `@type: Organization` with:

- name + alternateName ("FRQNCY", "FRQNCY Network")
- logo (ImageObject pointing at favicon.svg)
- description (matching the homepage meta description's spirit)
- founder (Person → Orlando Eisenreich, linked to /people/orlando/)
- foundingDate "2024"
- areaServed "Worldwide"
- knowsAbout (10 topical strengths)
- sameAs (Twitter only for now — Phase 5.3 expands this)
- publishingPrinciples (points at /editorial-standards/ — published in Phase 3.4)

## Why this matters

This is the schema feeding Google's Knowledge Graph algorithm. With Wikipedia + Wikidata (Phase 5) and consistent sameAs (Phase 5.3), this evolves into a knowledge panel for "FRQNCY" queries.

## Verification

`grep -c '"@type":"Organization"' /Users/orli/Documents/Claude/Projects/FRQNCY\ WEBSITE/index.html` returns 2 (one is the Person.worksFor parent for founder; one is the Organization block itself).

Test on https://search.google.com/test/rich-results once the change is live.
