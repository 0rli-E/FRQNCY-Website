# FRQNCY Handoff — 2026-06-11
> For the next Claude session. Read this before reading CLAUDE.md. It supersedes the stale figures in memory.

---

## What happened this session

Two tasks completed back-to-back.

**Task 1 — Master Roadmap.** 10 agents dispatched simultaneously across every file and data source in the repo. Each agent took a non-overlapping domain slice: roadmap proposals, protocol/identity, website structure, SEO, NRG platform, harness, content inventory, infra/mobile, personas/commerce, distribution. Their returns were synthesised into `FRQNCY-MASTER-ROADMAP.html` (now at `/Users/orli/Documents/Claude/Projects/`). Key finding: memory files were stale — actual live counts are 227 topics, 1,161 resources, 21 Supabase migrations. The roadmap reflects the live numbers.

**Task 2 — Analytics system.** Orlando chose Cloudflare-native (Supabase + Cloudflare Pages Functions, no third-party tools). Four files written, six pages wired.

---

## Files created this session

| File | Purpose |
|---|---|
| `FRQNCY-MASTER-ROADMAP.html` | Full platform roadmap, dark theme, 7 tracks, operator queue |
| `supabase/migrations/022_analytics_events.sql` | Append-only event log, RLS, GIN index on properties JSONB |
| `functions/api/analytics.js` | Cloudflare Pages Function event collector |
| `assets/frqncy-analytics.js` | Client-side tracker, 24 event types, DNT-respecting |
| `FRQNCY-ANALYTICS-DASHBOARD.html` | Local HTML dashboard, Chart.js, Supabase service-role query |

All five are in `/Users/orli/Documents/Claude/Projects/` (roadmap + dashboard) and in the FRQNCY WEBSITE repo (migration + function + tracker).

## Pages wired with the analytics script tag

`index.html`, `explore.html`, `my-frqncy.html`, `chart.html`, `my-frqncy/dashboard/index.html`, and `generate.js` (so every generated topic page picks it up automatically on the next build run).

---

## Operator actions still required

Nothing from this session is fully live until you do these in order:

1. **Run migration 022** in the Supabase SQL editor (`supabase/migrations/022_analytics_events.sql`). Idempotent, safe to run twice.
2. **Confirm env vars are set** on Cloudflare Pages: `PUBLIC_SUPABASE_URL` and `SUPABASE_SERVICE_ROLE_KEY` must exist for the analytics function to write. The subscribe function uses the same vars, so they're almost certainly already there.
3. **`git push`** — sandbox can't push; the analytics script tags are sitting in local files only.
4. **Open the dashboard** (`FRQNCY-ANALYTICS-DASHBOARD.html` at the Projects root) and enter your Supabase URL + service_role key. Credentials go to sessionStorage only, cleared on tab close.

The tracker is fire-and-forget (always returns 200) so it won't break anything if the migration hasn't run yet — events just fail silently.

---

## Existing operator queue (pre-this-session, still open)

From `NRG-LAUNCH-CHECKLIST.md` — none of these were touched this session:

- Apply Supabase migrations 002, 003, 006, 007, 008, 009, 010, 011–021 as needed
- Set `PUBLIC_PRIVY_APP_ID`, `RESEND_API_KEY`, `RESEND_FROM` on Cloudflare Pages
- `cd social-src && rm -rf node_modules package-lock.json && npm install && npm run build && cp -r dist/* ../social/`

---

## Current platform state (verified against actual files)

**Content graph:** 227 topics across 18 domains, 1,161 resources (books/people/tools/orgs/media), 1,100+ live topic URLs. Generator at `generate.js` + `scripts/generate_topic_page.py` — the BESPOKE-marker rule is in effect (no direct HTML injection into `/v2/<topic>/` or item pages).

**NRG social (social-src/):** 21 migrations committed. E2EE libsodium messaging, Ed25519 post signing, Privy embedded wallets, Bluesky cross-post bridge + federated timeline, Nostr NIP-01 layer, membership Stripe, referral codes, practice tracker — all source-complete, blocked only on operator env vars + migration run.

**Harness:** `@frqncy-network/harness` v0.19.1-alpha.1 at `frqncy-harness/`. 33 personas (7 Council always Opus with `evolves:false`, CEO, 6 C-Suite, 19 Workers, Learning Agent). Telegram bridge live. Agent commerce (ERC-8004 + x402 on Base) uncommitted due to `.git/index.lock`. Sub-agents blocked by default (`proposals/SUB-AGENTS.md`).

**Mobile app:** `app/` is source-complete (Capacitor 7, all Kotlin classes compile clean). Waiting on APK build — `app/docs/SHIPPING-2026-04-29.md` has the terminal commands.

**Analytics:** migration 022 written but NOT yet applied. Script tag live on six pages in local files, not yet pushed.

---

## What's worth doing next

In rough priority order, pulled from memory + the roadmap synthesis:

1. **`git push`** — everything above is local. Push is the single highest-leverage action.
2. **Run migration 022** and open the dashboard. First real data.
3. **"For you" feed on NRG** — `getFeedPosts` in `api.ts` already does fan-out-on-read. Just needs a UI tab toggle (Global / Following) on `/social/`. One component change.
4. **Channel discovery surface** — recently-active channels by post count, editorial-values-compliant copy ("active threads" not "trending").
5. **Reading-time indicators on topic pages** — add to `generate.js`: word count → ~225 wpm estimate → render in hero eyebrow. Skip for stub pages.
6. **FAQ schema rollout** — #1 AI-citation signal (2.1× lift on Perplexity/ChatGPT, 3.2× on Google AI Overviews). Must go through the generator via BESPOKE markers. See `audits/seo/` for baseline.
7. **BLNC stablecoin testnet** — Q4 2026 per the 90-day plan.

---

## Critical invariants — do not break these

- **No leaderboards, no ranking, no "calls" framing** anywhere on the platform. Conviction as self-expression is fine; ranking people is not. `ConvictionLeaderboard.tsx` and `pages/leaderboard.astro` were deleted. Don't re-add.
- **BESPOKE markers or generator only** for any HTML going into `/v2/<topic>/index.html` or item pages (`books/`, `people/`, `orgs/`, etc.). Direct injection gets wiped on the next generator run. Full forensic at `audits/seo/runs/2026-04-29-CRITICAL-FINDING-build-pipeline.md`.
- **Sub-agents off by default** in the harness claude-sdk lane (`disallowedTools: ['Agent']`). Override per-call only after reading `proposals/SUB-AGENTS.md`.
- **Cooperation over competition** is infrastructure, not preference. It's in the code, the data model, the deleted leaderboard. Treat it as a hard constraint.

---

## Key reference files

```
proposals/EXECUTION-PLAN-90D.md   — canonical 90-day roadmap (2026-04-27 → 2026-07-26)
proposals/FRQNCY-VOICE-PLAYBOOK.md — read before writing any user-facing copy
proposals/EDITORIAL-VALUES-V2.md   — slogans, posture
proposals/BACKEND-STATUS.md        — alive / scaffolded / zero-state per surface
NRG-LAUNCH-CHECKLIST.md           — done + blocked-on-operator steps
audits/seo/                        — all SEO work, baseline, run logs
app/docs/SHIPPING-2026-04-29.md    — mobile app state + APK terminal commands
frqncy-harness/proposals/SUB-AGENTS.md — why sub-agents are off by default
```

---

## Sandbox constraints (so you don't waste time)

The Cowork sandbox cannot: run `npm install` (bindfs rename limitation), push to git (`.git/index.lock`), or access Supabase/Cloudflare/Stripe/Privy dashboards. All of those are Orlando's to run in terminal or browser. The sandbox can read, write, edit files and run arbitrary bash for non-npm tasks.
