# FRQNCY Site-Wide Bug Audit · 2026-05-22

Synthesised from 10 parallel agents. Sorted by severity. Tickbox shows whether fixed in the 2026-05-22 sweep.

## CRITICAL — broken functionality

- [x] `/v2/` URL leak in 7 topic pages (privacy, vibration, oneness, source, akashic-records, dimensions, synchronicity) — `og:url` and canonical pointed to `/v2/<slug>/` instead of `/<slug>/`
- [x] `/my-frqncy/index.html` missing — was referenced as nav CTA and from `/charts/` and `/practice/` side-nav
- [x] `crypto/explorer.html`, `crypto/projects.html` script/css paths used `../../` (overshot root)
- [x] `word-illuminator/` 6 pages used `../../../` paths (overshot root)
- [x] `courses/index.html` used `../../` for nav-dropdown.css, mobile-nav.js, chat-widget.js, favicon, manifest
- [x] `donate.html`, `launchpad.html`, `newsletter.html` missing `mobile-nav.js` + `chat-widget.js` (nav disappears at ≤720px)
- [x] `donate.html` 4 buttons (PayPal/Google Pay/Card/Bank-Transfer) all `href="#"`
- [x] `donate.html` `/launchpad/` link 404s (only `launchpad.html` exists)
- [x] `newsletter.html` posts to `/api/newsletter/subscribe` (endpoint missing)
- [x] `newsletter.html` ships public dev-note "Backend endpoint is a stub — wire to Klaviyo…"
- [x] `_chrome/global-header.html` had "FRQNY treasury multi-sig" typo — propagated to 314 pages
- [x] Same "FRQNY" typo in `donate.html`, `launchpad.html`
- [x] Mayan converter line 187 — Mar 21 cell wrong (was day 26 Thursday, correct is day 24 Tuesday)

## MAJOR — data integrity / visible defects

- [x] aligned-goods.json — 4 topicSlugs not in content.json (`grounding`, `wellbeing`, `longevity`, `recovery`) — added as topics
- [x] explore.html resource result links used `target="_blank"` for all internal URLs
- [x] explore-data.json — 3 ghost topic nodes (`t-husbandry`, `t-robert-gould`, `t-spirituality`) — removed
- [x] platform.html — "15 domains" vs "14 Domains" mismatch (resolved to 14)
- [x] places/bali + places/sedona — no external URLs added (deferred — places treated as informational hubs)
- [x] places/all/index.html meta description claimed "15 places" — confirmed; "Show all 15" button is correct
- [ ] All 7 places pages — no hero images (deferred to image-curation pass)
- [ ] entities.json — 34 papers missing from 912 entities (deferred — needs generate-entities pipeline fix)
- [ ] explore-data.json p-* vs places.json pl-* prefix mismatch (deferred — affects 18 nodes; needs generator change)
- [ ] assets/network-map.js HOME_NODES 194 nodes vs explore-data.json 288 (deferred — needs build-step extraction)
- [ ] Mini-maps for commerce/curation/places-domain call missing filterByDomain IDs (deferred — same fix)
- [ ] Social — duplicate astro-island uid="ZuTrsh" in social/index.html (deferred)
- [ ] Social — two `<nav>` bars stacked on every social page (deferred — Astro template change)
- [ ] about.html "Eight Pillars" vs start-here.html "six pillars" (deferred — editorial reconciliation, not just a fix)

## LOW — cosmetic / performance

- [ ] `b-gödel-escher-bach` unicode slug (deferred)
- [ ] index.css dead rules (`#quote-section`, `.intro-triad`, `#details-section`) — deferred cleanup pass
- [ ] Large image files (sylvan-numismatica.png 4.3MB, crypto-hero.png 2.8MB) — deferred WebP conversion
- [ ] Large inline scripts (watch/index.html 156KB, dashboard 74KB) — deferred extraction
- [ ] 18 books, 40 orgs, 19 media missing image fields — deferred backfill
- [ ] `aria-label` mismatch on index.html intro-tagline

## Confirmed non-bugs

- Service worker: 25 PRECACHE + 5 DATA_PRECACHE URLs all exist; install handlers correct
- Sacred Geometry v2 page: BESPOKE-LOCK present, Ana B'Koach Hebrew renders correctly
- All 7 bed JSONs parse; no duplicate IDs; counts match folder counts (people 312, books 317, orgs 117, places 15, media 73, music 8, papers 34)
- Mayan converter math correct for 2012-12-21 → 13.0.0.0.0 4 Ajaw 3 K'ank'in
- Schumann live image URL returns HTTP 200
- All marquee `leadIds` resolve to existing entities
- Supabase IS used in the social layer (`vyazlspbmwmlyncdlezh.supabase.co`) via Astro SSR chunks — `social-auth.js` / `social-config.js` are dead code, not currently included in any HTML

## Open architectural questions (for later sessions)

- `social-auth.js` / `social-config.js` — leftover from pre-Astro iteration; should be deleted or wired in
- Membership page is a topic-stub but nav advertises it — build the page or relabel the nav entry
- Pillar count: 6, 8, or 14 / 15 domains — pick a canonical number and align all marketing pages
