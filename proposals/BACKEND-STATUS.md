# FRQNCY — Backend Status

Single source of truth on what's alive / scaffolded / zero-state across every surface, **as of 2026-04-27**. Supersedes the surface-level summaries in `SETUP-CHECKLIST.md` and the older audit notes. Update this when reality moves.

Keep entries short. Each surface: one paragraph state + bullets for files + bullets for what's blocking "alive."

---

## Legend

- 🟢 **Alive** — deployed, accessible to a visitor, working end-to-end.
- 🟡 **Scaffolded** — code exists, blocked on deploy / migration / one wire-up step.
- 🔴 **Zero-state** — does not exist; needs to be built.
- ⚪ **Out of 90-day scope** — listed in `EXECUTION-PLAN-90D.md` Future Roadmap.

---

## Static site (main)

🟢 Alive. 146 topic pages under `v2/`, 770+ resources in `resources.json`, 695 URLs in sitemap. Auto-deploys on push via `.github/workflows/build.yml` + Cloudflare Pages.

- Build: `package.json` scripts (`build`, `build:kb`, `build:og`, `build:pages`, `build:courses`, `build:watch`).
- Topic graph: `search.json` (146 topics), `resources.json` (770+ resources), `entities.json` + per-entity dirs (`people/`, `books/`, `orgs/`, `media/`).
- Navigation: `v2/explore.html` is the topic graph spine. Header has Discover / Capital / Community dropdowns. No prev/next or mini-map on individual topic pages — that's a Phase 1 polish target.

### Topic-page state (important framing)

The long horizon: **every topic page is its own piece** — its own layout, typography, structural metaphor, colour, and visual language emerging from the subject. FRQNCY is an artwork; each topic is an expression. This is months-to-years of work, not a sprint.

Three states a topic page can be in:

- 🟢 **Book / commissioned** — substantively written and laid out specifically for this topic. About 6–10 pages today (meditation, manifestation, prosperity-mindset, history, etc.).
- 🟡 **Scaffolded — templated content block** — a 3-section template (frame / structure / practice) was inserted on 2026-04-28 to lift these out of stub state. Better than the bare 10-paragraph baseline, but they share a structural skeleton and are **placeholder, not finished**. They will be replaced with unique treatments one at a time as each is commissioned. About 35 pages today: detox, water, quantum-grammar, governance, future-cities, regenerative-farming, peace, collective-intelligence, biomimicry, systems-thinking, taoism, soul, oneness, synchronicity, akashic-records, dimensions, merkaba, remote-viewing, mythology, decentralized-networks, ar-vr, robotics, quantum-computing, biotechnology, future-tech, emergence, open-source, renewable-energy, solar, wind-energy, geothermal, energy-storage, bioenergy, grid-technology, energy-policy, visual-art, dance, architecture, fashion, photography, theater, film, poetry.
- 🔴 **Stub** — auto-generated 334-line / 10-paragraph baseline only. No content beyond the topic name and curated resources. About 50 pages remain. **Stays as stub** until each gets its own commissioned treatment.

`scripts/insert-topic-content.py` exists in the repo as a record of the templated-block approach but **will not be re-run**. Future topic-page work happens one page at a time, on demand, with structural decisions specific to that topic.

## AI chat widget

🟢 Alive. `chat-widget.js` (26 KB) + `functions/api/chat.js` + auto-built KB. Uses Cloudflare Workers AI (Qwen 30B-A3B) — **free**, no API key. Anthropic key only required if you want a paid fallback path.

- Knowledge base: `functions/api/_kb.js`, regenerated by `build-kb.js` on every build.
- Rate-limit: 20 req/min per IP.
- Needs the `AI` binding set in Cloudflare Pages env (one-click in dashboard).

## Supabase project (active)

🟡 Scaffolded. There are **two parallel migration trees** in the repo — only the root one is live.

- **Active:** `/supabase/migrations/001_social_schema.sql` (612 lines, full social schema) + `002_fix_conversation_rls.sql` (DM RLS recursion fix) + `003_subscribers_charts_storage.sql` (subscribers, charts, storage buckets).
- **Older / superseded:** `/social-src/supabase/migrations/002_conviction.sql` + `003_search_and_leaderboard.sql`. Need to fold into the active set or formally deprecate. **Action:** decide and clean up by end of Phase 1.
- Project ID: `vyazlspbmwmlyncdlezh`.
- Migrations 002 + 003 **not yet applied** — must run manually in Supabase SQL editor (per `SETUP-NEXT-STEPS.md`).
- Service-role key not yet set in Cloudflare Pages env.

## Social platform (`social-src/`)

🟡 Scaffolded. Astro 5.7 + Preact + Supabase + Cloudflare Pages target. Components are built; deploy is one wire-up step.

- Pages: index, login, profile/index, profile/[username] (renamed `.old`), bookmarks, messages, leaderboard (intentionally redirects to /social/space — non-ranking), search, notifications, space/{index,roadmap,research}.
- Components built: AuthForm, AuthProvider, NavAuth, MobileNavAuth, Feed, PostCard, PostComposer, ConvictionToggle, ConvictionCard, ProjectPicker, ProjectTagInput, ProjectBadge, CommentForm, CommentItem, CommentsThread, ProfilePage, ProfileCard, ProfileStats, FollowButton, MessageInput, ConversationsList, ChatWindow, StartConversationButton, NotificationsList, BookmarksView, SearchView, ConvictionLeaderboard (returns null — kept as a dead stub with comment).
- Lib: `supabase.ts` (client), `api.ts` (server-side calls), `useMessages.ts` (DM hook), `projects.ts` (project tagging).
- **Blocking "alive":** apply migrations 002+003 in Supabase, set `PUBLIC_SUPABASE_URL` + `SUPABASE_SERVICE_ROLE_KEY` in Cloudflare Pages, deploy `/social/*` route.
- **Editorial flag:** `pages/leaderboard.astro` redirects to `/social/space` — verified compliant with cooperation-over-competition rule.

## Sanctuary / My FRQNCY dashboard

🟡 Scaffolded — but **much further along than first audit suggested**.

- `my-frqncy.html` (1,435 lines): multi-step intake form (birth data, sign selection). Currently local-storage only.
- `my-frqncy/dashboard/index.html` (**2,360 lines**): the real Sanctuary surface. Dream, chief aims, objectives, goals, habits, scoreboards, vision board. Imports Supabase via `assets/frqncy-supabase.js` for cloud sync.
- `assets/frqncy-supabase.js` (285 lines): shared Supabase client for vanilla HTML pages — auth, sanctuary cloud sync, charts, dreams.
- **Blocking "alive":** the same migration apply + env vars as social platform. After that, logged-in users get cross-device sync; anonymous visitors keep local-storage only.
- Personalisation engine (filter resources by HD type / authority / sun gate) is **not yet wired** — Phase 2 work.

## Charts (Human Design / Gene Keys / Natal)

🟢 **Alive and calibrated.** Earlier audit got this wrong — the chart generator is fully working today, not blocked.

- `chart.html` (522 lines) + `chart.js` (1,565 lines): the production chart engine. Three tabs (HD / Gene Keys / Natal). Sub-arcsec planetary positions via `astronomy-engine` with a full Meeus fallback if the CDN fails. Wheel offset baked at **1.83°**, calibrated against Jovian Archive (26/26 gates match on the v1 fixture).
- **Human Design:** type, profile, authority, defined centres, all 26 activations across Personality + Design, plus a full inline bodygraph SVG (9 centres, 64 gates, 36 channels). PDF export wired.
- **Gene Keys:** complete Hologenetic Profile per Richard Rudd's framework — 11 spheres across three sequences (Activation, Venus, Pearl), each with Shadow/Gift/Siddhi data baked in for all 64 keys.
- **Natal:** birth chart layer using the same planetary calculation.
- `workers/hd-reading.js` (223 lines): Cloudflare Worker for AI HD reading. **Not yet deployed** — needs the `AI` binding set in Pages and a `wrangler deploy`. The chart still works without it; the AI reading is an upgrade.
- `chart-v2/` is a refactor project (cleaner ES modules, tighter calibration harness), not a prerequisite. The production engine in `chart.js` works today. Treat `chart-v2/` as a Phase 5+ refactor to consolidate the inline engine.
- **The only "alive" gap left here is wiring the AI reading worker** — that's a Phase 2 Day 1 deploy step, not new code.

## Watch (video curation)

🟢 Alive. 6 teachers (Neville Goddard 20 latest, Osho, Sadhguru, Sai Maa, Gary Spivey, Kevin Trudeau).

- `videos.json` (766+ entries), `playlists.json`, `providers.json`.
- `generate-watch.js` (150 lines) inlines data into `v2/watch/index.html` at build time.
- **Auto-ingestion:** does not exist. Phase 4 plan = assisted ingestion via YouTube Data API + harness agent that proposes PRs.

## Audio

🟢 Alive. The Strangest Secret (Earl Nightingale, 1956) added recently. Linked under Discover → Audio in the header dropdown.

## Courses

🟢 Alive as content. 5 full courses in `courses.json` (Meditation 101, Quantum Reality, Conscious Living Foundations, Quantum Grammar, Crypto Fundamentals). Pages render via `generate-courses.js`.

- No enrollment, no completion tracking, no payments. Phase 3 picks one course (likely Quantum Grammar) for the first paid experiment at $19.
- No `pricing` field in `courses.json` schema yet — added in Phase 3.

## Aligned Goods

🟢 Alive as curated list. 40 entries in `aligned-goods.json`, all `affiliate: false`.

- No checkout. No `revenue_relationship` field yet.
- Phase 3 adds the disclosure layer + field, then Phase 6 adds crypto-tip deep-links (no custody).

## Mailing list

🟡 Scaffolded. Forms POST to Substack (`frqncy.substack.com`). Brevo path documented but not wired.

- `subscribers` table arrives with migration 003 (Supabase).
- Resend (`re_…`) optional for welcome emails.
- Phase 1 Day 7 decision: stay on Substack vs wire Brevo + Resend + Supabase. Default = wire all three so referral codes can attach.

## Referrals

🔴 Zero-state. `referral_code: ''` stub fields exist on `podcast.html` and `space.html` subscribe forms. No code generation, no capture, no rewards.

- Phase 1 Day 5 builds plumbing (generate code, capture `?ref=XYZ`, store on user record).
- Phase 3 Week 6 builds rewards (3 → free month, 10 → quarterly gathering invite, 25 → founder badge).

## Membership

🔴 Zero-state.

- No `/membership/` page. No tiers. No Stripe.
- REVENUE-MODEL.md sketches tiers (Drop-in / Local / Network / Founding) for Spaces — those wait until a Space has a date.
- Phase 3 Week 5 ships **one** tier ("Network member") via Stripe test → live.

## Goods (orderable)

⚪ Out of 90-day scope as a checkout. Aligned Goods stays curated-link only. Phase 6 adds crypto-tip deep-links to teacher profiles + Aligned cards (no custody, free to implement).

## Crypto value

🟡 Mostly content; functional integration deferred.

- 28 topic pages under `v2/crypto/`. `crypto-projects.json` exists. `v2/crypto/projects.html` lists projects.
- No wallet connect, no payments, no on-chain.
- Phase 5 adds the **content** crypto values: "Crypto is freedom technology" slogan, Bitcoin values list (borderlessness, immutability, etc.), Ethos featured project, Obi profile, Mike Maloney money attributes, Kevin Trudeau canon.
- Phase 6 adds the cheap functional surface: Solana Pay / Ethereum deep-link tip buttons. No custody.

## Luma events

🔴 Zero-state.

- No embed, no sync.
- ⚪ NS (Norman's Space / first physical Sanctuary) operations are out of 90-day scope. When a Space has a date, Luma integration takes < 1 day.

## Fund

🟡 Stub directory `v2/fund/index.html` exists.

- Phase 3 Week 6 fleshes out the public thesis page (no deal flow shown).

## Live space (`space.html`)

🟡 Static info page. No booking. No calendar.

- ⚪ Operations (lease, ops budget, tier pricing) out of 90-day scope.

## Land / model city / Lugano / Próspera

⚪ Out of 90-day scope as operations. Phase 5 adds **topic pages** for each so the teaching lives on the site.

## Mobile app (`app/`, Capacitor 7)

🟡 Scaffolded. 5 stub pages (wake, sleep, bedside, alarm, settings). Vite + Capacitor configured. iOS/Android shell.

- `app/src/native/frqncy-alarm.ts`: native alarm wrapper.
- `app/src/lib/sync-manager.ts`: stub.
- Phase 6 replaces stubs with real screens (my-frqncy, chart, chat, practice-log, settings) + embeds the AI chat widget natively.

## MCP server (`mcp-servers/frqncy-content/`)

🟢 Alive. v0.1.0. 11 tools: search_topics, get_topic, list_topics, list_domains, list_pillars, search_resources, get_resource, list_resources_for_topic, list_resources_by_type, random_topic, stats. Tested live via `frqncy-harness mcp test frqncy-content` ✓.

- Wired into `~/.frqncy-harness/mcp.json` and confirmed responding.
- Phase 4 Week 8 extends with three **write** tools (`propose_resource`, `propose_topic_addition`, `propose_topic_edit`) that open PRs to the website repo. Manual merge always.

## Harness (`@frqncy/harness`)

🟢 Alive at v0.7.0-alpha.1. 7 lanes (anthropic, openai, google, openrouter, chutes, claude-code, codex). 7 tools (bash, read, write, grep, glob, web_fetch, web_search). MCP client. gtr+tempdir sandbox. Trace store with auto-commit-and-push hook. $5/$25 per-conversation cost caps. Lethal-trifecta gate. 202 tests passing.

- Replay command for manual eval. **No autonomous self-optimisation loop yet** — Phase 4 plants the seed via `scripts/reflect-traces.js`.
- Per-day / per-month cost aggregates not implemented — Phase 4 patches.

## Auto-grow loops

🟢 Alive at v0. Three nightly draft scripts in `scripts/auto-grow/` + GitHub Actions workflow at `.github/workflows/auto-grow.yml`. Per `proposals/AUTO-GROW-LOOPS-V0.md`. Editorial gate intact — nothing auto-merges.

- `resource-suggest.mjs` reads recent NRG posts via Supabase, drafts `resources.json` candidates with source-post context. Exits 0 on missing secrets so the workflow never fails loudly.
- `video-ingest.mjs` scrapes `c-teachers` playlist channel pages, drafts new `videos.json` IDs. No API key needed.
- `trace-reflect.mjs` (local-only, not in workflow) writes a Markdown report from `~/.frqncy-harness/traces/` covering volume, cost, top failure modes.
- Workflow opens a PR via `peter-evans/create-pull-request@v6` when output changes. Schedule: 08:00 UTC daily + manual `workflow_dispatch`.
- **Blocking full output:** `SUPABASE_URL` and `SUPABASE_SERVICE_ROLE_KEY` GitHub Actions secrets are not set, so `resource-suggest` will exit-0 silently each night until they are. `video-ingest` runs unblocked.

## Build / deploy

🟢 Alive. GitHub Actions (`.github/workflows/build.yml`) regenerates content on push. Cloudflare Pages auto-deploys.

- `_redirects` (70 lines): clean URLs, 404 fallback, internal-doc blocking.
- No pre-flight tests / link audits in CI — manual `link-audit.md` only.

## Editorial / standards docs (state of writing, not code)

🟡 Live but uneven.

- `CLAUDE.md` — refreshed today (2026-04-27) for harness v0.7 and added EXECUTION-PLAN / VISION / BACKEND-STATUS doc references.
- `proposals/EDITORIAL-VALUES-V2.md` — codified.
- `proposals/EXECUTION-PLAN-90D.md` — locked today.
- `proposals/EDITORIAL-STANDARDS.md` — Phase 3 Week 5 deliverable. Doesn't exist yet. Required precondition for scaling Aligned/Fund/sponsored content.

---

## Outstanding cleanups (Day 1 follow-ups)

- [ ] Decide fate of `/social-src/supabase/migrations/002_conviction.sql` + `003_search_and_leaderboard.sql` (older parallel set). Likely: delete both, leave a README pointing at root `/supabase/migrations/`.
- [ ] Apply migrations 002 + 003 in Supabase SQL editor.
- [ ] Set `PUBLIC_SUPABASE_URL` + `SUPABASE_SERVICE_ROLE_KEY` in Cloudflare Pages env (Production + Preview).
- [ ] (Optional) Add `RESEND_API_KEY` + `RESEND_FROM` for welcome emails.
- [ ] Set `AI` binding in Cloudflare Pages for the chat widget (one click).
- [ ] Push current uncommitted work (M places/index.html, M explore-data.json, several `places/*` new dirs) before Phase 1 Day 2 work begins.

## Surprises that contradict prior audit

1. The **active** Supabase migration set is at root `/supabase/migrations/`, not under `social-src/`. The `social-src/` set is older.
2. The Sanctuary dashboard at `/my-frqncy/dashboard/` is **2,360 lines and already wired with Supabase** — not zero-state. Just blocked on the same deploy step as social.
3. `assets/frqncy-supabase.js` provides a vanilla-HTML Supabase client, so chart.html / my-frqncy.html / index.html / dashboard can all auth + sync without going through Astro.
4. The leaderboard surface was already neutralised (page redirects, component returns null with comment) — no new editorial cleanup needed.
5. The chat widget uses **free Cloudflare Workers AI**, not the Anthropic API. The `ANTHROPIC_API_KEY` mentioned in `SETUP-CHECKLIST.md` step 4 is misleading — only set it if you want a paid fallback.
