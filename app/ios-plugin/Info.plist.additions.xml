<!--
  Additions to Info.plist required by FrqncyAlarm on iOS.
  Merge these into ios/App/App/Info.plist after `npx cap add ios`.
-->

<!-- Background modes: audio lets the alarm ring when locked. -->
<key>UIBackgroundModes</key>
<array>
    <string>audio</string>
    <string>fetch</string>
</array>

<!-- Required so AVPlayer can stream audio from Cloudflare R2/Stream -->
<key>NSAppTransportSecurity</key>
<dict>
    <key>NSAllowsArbitraryLoadsForMedia</key>
    <true/>
</dict>

<!-- Privacy usage descriptions (keep minimal — only add keys you actually use) -->
<!--
<key>NSMicrophoneUsageDescription</key>
<string>FRQNCY uses the microphone for optional ambient sleep tracking.</string>

<key>NSHealthShareUsageDescription</key>
<string>FRQNCY reads sleep phase data to time your wake window at the gentlest moment.</string>
-->
